This folder contains all source code that has been implemented to act with the IBM Cloudant database.

The HTML files set the template for the page, the JavaScript allows PUT and GET requests, and a css file (main.css) formats the HTML.